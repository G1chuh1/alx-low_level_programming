#include <stdio.h>
#include <string.h>

int main() {
  //initialising strings
  char str1[] = "programming is like building a multilingual puzzle"

    //writting to stdout
    puts(str1);

  return 0;
}
